# Editor and OS noise
*.swp
*~
.DS_Store

# Local scratch space for screenshots and experiments that should not ship
scratch/

# Natural Earth sources, downloaded by tools/build-basemap.py. Around 70 MB,
# and the generated data/basemap.bin is what actually ships.
tools/.cache/

# Python runtime bytecode cache
__pycache__/
